1. 폴더

0) root - 바깥 파일

0-1) .env
- 환경변수 파일
- 비밀정보를 담고, 변수로 설정(비밀 설정 정보 관리)

0-2) gitignore
- git에 올라가면 안되는 파일명쓰기
- git에 쓴 파일명 제외후 올라감

---------------------------------------------------------------------------------------------

1) client 폴더
=> 프론트엔드 폴더

----------------------------------------------------------------------------------------------

2) server 폴더 
=> 백엔드 폴더

2-1) config 폴더

=> index.js
- dotenv 라이브러리 통해서 env 파일을 환경변수에 대신 설정
- Node.js 기반이라면 process.env[key] 형태(process.env.NODE_ENV)

-> dotenv 라이브러리
- 환경변수 파일을 외부에 만들고, 관리
- .config() 함수에 path를 지정하면 다른 위치에 있거나 기본 이름이 다른 .env 파일을 참조

2-2) middleware 폴더
=> auth.js - 토큰 인증
- token을 불러옴
- token이 없을 경우
- token이 있을경우
- token이 존재하더라도 에러발생할 경우

----------------------------------------------------------------------------------------------

3) models 폴더
=> category.js
- CategorySchema 정의
- 카테고리

=> comment.js
- CommentSchema 정의
- 댓글 부분
- 필수 부분 설정하기 - required: true