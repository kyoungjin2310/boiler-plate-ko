1. 리덕스를 쓰지 말아야 할 때 - 대처
=> input일 경우 - 타자칠때마다 리렌더링됨
=> 리덕스를 input에서 꼭 사용해야할 경우 onBlur 사용
-> onBlur 이벤트 - 포커스를 잃었을때 이벤트가 발생

2. 타자를 칠때 버벅일 정도일 경우
=> user같은 경우 철자가 바뀔경우 계속 리렌더링 되고 과부하 걸릴 수 있음
=> return이 객체가 아니고 문자열이 나오게끔 하기
=> 예시
const email = userSelector((state) => state.user.email);
const password = userSelector((state) => state.user.password);
const list = userSelector((state) => state.post.list);
const dispatch = useDispatch();